# Generated by `theme` from themes/{{name}}.json -- edits will be overwritten.
#
# Colours only. The status line layout that consumes these lives in the
# tracked .config/tmux/statusline.conf.

set -g @thm_name "{{name}}"

set -g @thm_rosewater "{{rosewater}}"
set -g @thm_flamingo "{{flamingo}}"
set -g @thm_pink "{{pink}}"
set -g @thm_mauve "{{mauve}}"
set -g @thm_red "{{red}}"
set -g @thm_maroon "{{maroon}}"
set -g @thm_peach "{{peach}}"
set -g @thm_yellow "{{yellow}}"
set -g @thm_green "{{green}}"
set -g @thm_teal "{{teal}}"
set -g @thm_sky "{{sky}}"
set -g @thm_sapphire "{{sapphire}}"
set -g @thm_blue "{{blue}}"
set -g @thm_lavender "{{lavender}}"
set -g @thm_text "{{text}}"
set -g @thm_subtext1 "{{subtext1}}"
set -g @thm_subtext0 "{{subtext0}}"
set -g @thm_overlay2 "{{overlay2}}"
set -g @thm_overlay1 "{{overlay1}}"
set -g @thm_overlay0 "{{overlay0}}"
set -g @thm_surface2 "{{surface2}}"
set -g @thm_surface1 "{{surface1}}"
set -g @thm_surface0 "{{surface0}}"
set -g @thm_base "{{base}}"
set -g @thm_mantle "{{mantle}}"
set -g @thm_crust "{{crust}}"
